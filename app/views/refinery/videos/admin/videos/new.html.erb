<%= link_to t('.use_files'), refinery.new_videos_admin_video_path, 
  :class => 'select_video_type' << (@embedded ? '' : ' selected') if Refinery::Videos.use_type[:use_file] %>
<%= link_to t('.use_embedded_video'), refinery.new_videos_admin_video_path(:embedded => true), 
  :class => 'select_video_type' << (@embedded ? ' selected' : '') if Refinery::Videos.use_type[:use_embed] %>
<%= render 'form' %>
